// Declare a variable named projectName with an explicit string type.
// Log that variable to the console.

let projectName: string = "receiving-discrepancy-tracker";
console.log(projectName);

/*
function logProject(projectName: string): void {
    console.log("Initializing ", projectName.toUpperCase());
}

logProject(projectName);
*/